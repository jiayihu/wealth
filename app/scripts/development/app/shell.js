var app = window.app || {};

app.shell = (function(window) {
  var config = {

  },
  stateMap = {

  };


  var initModule = function(container) {

  };

  return {
    initModule: initModule
  };

})(window);
