var app = window.app || {};

app.shell = (function(window) {
  var config = {

  },
  stateMap = {

  };


  var init = function() {
    app.views.you.init();
  };

  return {
    init: init
  };

})(window);
